Create a virtual environment in Python and install the following dependencies:

pip install PyQt5

pip install PyQtWebEngine

pip install folium

pip install geocoder

pip install pandas

pip install geopy

pip install matplotlib

pip install reportlab

pip install deap

pip install seaborn

pip install plotly

